export enum SETTING_PANEL {
  GENERAL = "general",
  ACCOUNT = "account",
}

export enum CONTACT_PANEL {
  CONNECTION = "connection",
  REQUEST = "request",
}

export enum PROFILE_PANEL {
  DESIGN = "design",
  INFORMATION = "information",
  FIELDS = "fields",
  PREVIEW = "preivew",
}
